@model IEnumerable<PWEB_P6.Models.Curso>

@{
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>@ViewData["Title"] <small>(<a asp-action="Create">Adicionar Curso</a>)</small></h1>

<form method="get">
    Pesquisa:
    <input type="text" id="TextoAPesquisar" name="TextoAPesquisar">
    <input type="submit" value="Pesquisar" />
</form>

<a asp-action="Index" asp-controller="Cursos" asp-route-disponivel="">Todos</a> |
<a asp-action="Index" asp-route-disponivel="true">Activos</a> |
<a asp-action="Index" asp-route-disponivel="false">Inactivos</a>

<table class="table table-bordered table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disponivel)
            </th>
            @*
            <th>
                @Html.DisplayNomeFor(model => model.Categoria)
            </th>
            <th>
                @Html.DisplayNomeFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNomeFor(model => model.DescricaoResumida)
            </th>
            <th>
                @Html.DisplayNomeFor(model => model.Requisitos)
            </th>
            <th>
                @Html.DisplayNomeFor(model => model.IdadeMinima)
            </th>
            *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disponivel)
            </td>
            @*
            <td>
                @Html.DisplayFor(modelItem => item.Categoria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoResumida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Requisitos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdadeMinima)
            </td>
            *@
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
